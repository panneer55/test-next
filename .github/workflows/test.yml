name: My First GitHub Action
on: [push]
jobs:
    test:
        runs-on: ubuntu—latest
        steps:
          - name: Checkout source code
            uses: actions/checkout@v2
          - name: run tree
            run: tree
