name: My First GitHub Action
on: [push]
jobs:
    test:
        runs-on: ubuntu—latest
        steps:
          - name: Checkout source code
            users: actions/checkout@v2
            run: tree
